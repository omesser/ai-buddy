{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the overlay window.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
