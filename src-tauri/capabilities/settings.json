{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "The Settings surface when opened as a webview needs to listen for settings-refresh events that fire when the roster changes while the window is open (the frame loop push path), and to emit settings_event commands when a row is edited. Native remains the default; this capability applies only when AI_BUDDY_SETTINGS_WEBVIEW=1.",
  "windows": [
    "settings"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
